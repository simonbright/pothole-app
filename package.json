{
  "name": "pothole-app",
  "version": "0.14.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "start:api": "API_ONLY=1 node server.js",
    "dev": "node server.js",
    "build:static": "mkdir -p _site && cp index.html review.html favicon.svg _site/"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2"
  }
}
